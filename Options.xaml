<ResourceDictionary
    x:Class="Cwseo.NINA.ManualFocuser.Options"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <DataTemplate x:Key="Manual Focuser_Options">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="6" HorizontalAlignment="Left">

                <!-- Header -->
                <StackPanel Grid.Row="1" Orientation="Horizontal">
                    <Path
                    Width="25"
                    Margin="5"
                    Data="{StaticResource ExclamationCircledSVG}"
                    Fill="{StaticResource ButtonForegroundBrush}"
                    Stretch="Uniform" />
                    <TextBlock VerticalAlignment="Center">
                        <TextBlock.Text>
                            Spkie Analysis Parameter
                        </TextBlock.Text>
                    </TextBlock>
                </StackPanel>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="70" />
                    </Grid.ColumnDefinitions>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- ROI Scale -->
                    <TextBlock Margin="0,2,6,2"
                        Text="ROI Scale"
                        Grid.Row="0"
                        VerticalAlignment="Center"
                        FontSize="11"
                        ToolTip="Recommended: 1.8 – 2.4&#x0A;Near focus → lower, narrowband → higher" />

                    <TextBox Margin="0,2,0,2"
                        Grid.Row="0"
                        Grid.Column="1"
                        Height="22"
                        FontSize="11"
                        HorizontalContentAlignment="Right"
                        VerticalContentAlignment="Center"
                        Text="{Binding RoiScale, StringFormat=F2}" />

                    <!-- Core Cut Fraction -->
                    <TextBlock Margin="0,2,6,2"
                        Text="Core Cut"
                        Grid.Row="1"
                        VerticalAlignment="Center"
                        FontSize="11"
                        ToolTip="Recommended: 0.12 – 0.20&#x0A;Controls spike core exclusion" />

                    <TextBox Margin="0,2,0,2"
                        Grid.Row="1"
                        Grid.Column="1"
                        Height="22"
                        FontSize="11"
                        HorizontalContentAlignment="Right"
                        VerticalContentAlignment="Center"
                        Text="{Binding CoreCutFraction, StringFormat=F2}" />

                    <!-- Background Ring -->
                    <TextBlock Margin="0,2,6,2"
                        Text="BG Ring"
                        Grid.Row="2"
                        VerticalAlignment="Center"
                        FontSize="11"
                        ToolTip="Recommended: 0.68 – 0.75&#x0A;Background estimation radius" />

                    <TextBox Margin="0,2,0,2"
                        Grid.Row="2"
                        Grid.Column="1"
                        Height="22"
                        FontSize="11"
                        HorizontalContentAlignment="Right"
                        VerticalContentAlignment="Center"
                        Text="{Binding BgRingFraction, StringFormat=F2}" />

                    <!-- Min Star Size -->
                    <TextBlock Margin="0,2,6,2"
                        Text="Min Star (px)"
                        Grid.Row="3"
                        VerticalAlignment="Center"
                        FontSize="11"
                        ToolTip="Recommended: 6 – 7 px&#x0A;Seeing dependent" />

                    <TextBox Margin="0,2,0,2"
                        Grid.Row="3"
                        Grid.Column="1"
                        Height="22"
                        FontSize="11"
                        HorizontalContentAlignment="Right"
                        VerticalContentAlignment="Center"
                        Text="{Binding MinStarSizePx, StringFormat=F0}" />

                    <!-- Saturation -->
                    <TextBlock Margin="0,2,6,2"
                        Text="Saturation"
                        Grid.Row="4"
                        VerticalAlignment="Center"
                        FontSize="11"
                        ToolTip="Recommended: 0.92 – 0.94&#x0A;Fraction of full well" />

                    <TextBox Margin="0,2,0,2"
                        Grid.Row="4"
                        Grid.Column="1"
                        Height="22"
                        FontSize="11"
                        HorizontalContentAlignment="Right"
                        VerticalContentAlignment="Center"
                        Text="{Binding SaturationLevel, StringFormat=F3}" />

                    <!-- Max Stars -->
                    <TextBlock Margin="0,2,6,2"
                        Text="Max Stars"
                        Grid.Row="5"
                        VerticalAlignment="Center"
                        FontSize="11"
                        ToolTip="Recommended: 4 – 6&#x0A;Lower for clusters, higher for galaxies" />

                    <TextBox Margin="0,2,0,2"
                        Grid.Row="5"
                        Grid.Column="1"
                        Height="22"
                        FontSize="11"
                        HorizontalContentAlignment="Right"
                        VerticalContentAlignment="Center"
                        Text="{Binding MaxStars, StringFormat=F0}" />

                </Grid>
            </StackPanel>
        </ScrollViewer>
    </DataTemplate>

</ResourceDictionary>
